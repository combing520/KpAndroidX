<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <app.cloud.ccwb.cn.linlibrary.image.RoundAngleImageView
        android:id="@+id/homeUrlImg"
        android:layout_width="match_parent"
        android:layout_height="0px"
        android:scaleType="centerCrop"
        app:border_width="0dp"
        app:layout_constraintDimensionRatio="690:388"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:roundHeight="6dp"
        app:roundWidth="6dp" />

    <TextView
        android:id="@+id/homeUrlTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:gravity="left|top"
        android:lineSpacingExtra="3dp"
        android:maxLines="2"
        android:paddingVertical="@dimen/margin_small"
        android:textColor="@color/home_tab_txt_color"
        android:textSize="@dimen/fontSize_middle"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/homeUrlImg" />

</androidx.constraintlayout.widget.ConstraintLayout>